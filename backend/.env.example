PORT=5000
JWT_SECRET=s1Pr09123ckt!
ACCESS_EXPIRES=15m
PASSWORD_PEPPER=u293812aksjxc!
MONGO_URI=mongodb://localhost:27017/nextransfer
FRONTEND_URL=http://localhost:3000
DB_REQUIRED=false
DB_MAX_RETRIES=3
DB_RETRY_DELAY_MS=1000
DB_SELECTION_TIMEOUT_MS=5000

# --- Email (SMTP) ---
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=465
EMAIL_USER=canvietexchange@gmail.com
EMAIL_PASS=8077097227CanVietExchange.
EMAIL_APP_PASSWORD=ebfh dlip absc defb
EMAIL_FROM=CanViet Exchange <no-reply@canviet.exchange>
# In dev, mock SMTP (no network calls). When true, verify/sendMail will succeed without contacting a server.
EMAIL_DEV_MODE=false

# --- Sessions ---
# Used to sign the session cookie; set to a long, random string in production
SESSION_SECRET=change-this-in-prod

# --- OTP settings ---
OTP_LENGTH=6
OTP_TTL_SECONDS=300


# --- Rate limits ---
RATE_LOGIN_MAX=10
RATE_REGISTER_MAX=20


# --- API Exchange Key ---
NEXT_PUBLIC_EXCHANGE_API_KEY=d81887bddb49a53767ca1cf9